%% @yash0307, @halwai

% This file contains random search function.
% Input parameters.
% -> Mapping Matrix.
% -> Eval Matrix.
% -> Underconsideration indexes.
% -> maximum range of squares.
% -> factor of reduction.
function [x y] = randomSearch(Mapping, Eval, indexes,rs_max_window, rs_reduction_factor)
    
end